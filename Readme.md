### Docker Setup for Lumen Development ###

Containerized PHP Stack.

** Helpful Commands **

run from the app directory to install composer requirements
`docker run --volume=$PWD:/app composer install`